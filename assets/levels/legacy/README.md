# Archived room campaign

These files are not loaded by the continuous-world runtime.

`room-01.local.level` is a byte-for-byte snapshot of the locally edited first room taken before
the 18-screen campaign migration. It is retained only to preserve pre-redesign work.
